<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29"/>



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LavenderLang"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31" >
        <activity
            android:name=".MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LanguageActivity" />
        <activity android:name=".InformationActivity" />
        <activity android:name=".DictionaryActivity" />
        <activity android:name=".GrammarActivity" />
        <activity android:name=".PunctuationActivity" />
        <activity android:name=".WritingActivity" />
        <activity android:name=".WordFormationActivity" />
        <activity android:name=".GrammarRuleActivity" />
        <activity android:name=".TranslatorActivity" />
        <activity android:name=".WordActivity" />
    </application>

</manifest>